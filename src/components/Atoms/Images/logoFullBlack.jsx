import React from "react"
import styled from "styled-components"

const LogoFullBlack = () => {
  return <Image src="/footer-logo.svg" />
}

const Image = styled.img`
  max-width: 230px;
`

export default LogoFullBlack
